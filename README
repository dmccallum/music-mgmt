Simple rake-based music encoding and relocation scripts. Assumes WAVs and FLACs have already been generated. Then just:

  %> rake -f src/rakefile.rb all

Obviously the default config is env-specific.
